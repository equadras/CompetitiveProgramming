#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL_DEBUG
#include "debug.h"
#else
#define debug(...)
#endif

#define endl "\n"
typedef long long ll;

const int maxn = 1e5+5;
tuple<int,int,int> adj[maxn];


void dsu (int u){
         
}

signed main(){
    ios_base::sync_with_stdio(0);cin.tie(0);
    int n, m, s; cin >> n >> m >> s;

    for (auto &[w, u, v]: adj){
        cin >> w >> u >> v;
    }

    return 0;
}


