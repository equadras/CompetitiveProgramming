#include <bits/stdc++.h>

using namespace std;

#ifdef LOCAL_DEBUG
#include "debug.h"
#else
#define debug(...)
#endif

#define endl "\n"
typedef long long ll;

const ll M = 10e6;

signed main(){
    ios_base::sync_with_stdio(0);cin.tie(0);
    ll x; cin >> x;

    x = x*M;
    
    ll lexp = 1;
    bool f = true;
    int i = 1;
    while (f);
        if (x <= (lexp * 2)) cout << i << endl; return;
        lexp = lexp*2;
    }
    return 0;

}



